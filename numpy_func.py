# Numpy vstack

# NumPy vstack 是一个将 Numpy 数组组合在一起的工具。
# 它本质上是 NumPy 中的数据操作工具。
# Numpy vstack 实际上是用于组合 Numpy 数组的几个 Numpy 工具之一。Numpy vstack、Numpy hstack 和 Numpy concatenate 都有些相似。例如，NumPy 连接是一个非常灵活的工具，可以将 NumPy 数组垂直或水平组合在一起。然后是 NumPy hstack，它使您能够将数组水平组合在一起。
# NumPy vstack 与 NumPy concatenate 和 NumPy hstack 相关，除了它使您能够将 NumPy 数组垂直组合在一起。

# 所以它有点像 np.hstack 的兄弟。NumPy hstack水平组合数组，NumPy vstack垂直组合数组。
# 既然你知道了 NumPy vstack 的作用，那么让我们看一下语法。

# NUMPY VSTACK 语法
# NumPy vstack 的语法非常简单。
# 通常，我们会使用 name 调用函数np.vstack()，但具体如何调用它取决于您如何导入 NumPy 模块。
# 如果您使用代码导入 NumPy import numpy as np，那么您可以在语法中使用别名引用 NumPy np。这是一个常见的约定，所以我将在此处使用它（尽管我还将将该函数称为 numpy.hstack）。
# 说了这么多，让我们仔细看看语法。
# NP.VSTACK 的语法
# 正如我在前几段中提到的，np.vstack 的语法非常简单。
# 您使用代码调用函数np.vstack()，然后在函数内部，我们需要提供要组合的数组的名称。